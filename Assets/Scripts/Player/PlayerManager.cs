using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerManager : MonoBehaviour
{
private bool turn;

    // Set units active by iterating through dictionary 
    // Boolean active true or false 
    // Will not be using dictionary. Will be using Arrays 
    // Dictionary <gameObjectId,key> dict = new Dictionary <gameObjectId,key>;

public void CreateUnit(string type)
	{
        // Code for creating a through a dictionary  
        //if(type == "Infantry"){
            //dict.Add();
    }
}


